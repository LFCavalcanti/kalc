describe("The Keypad of the Calculator should...", () => {

    test("contain all buttons", () => {
        expect(true)
    })

    test("all number buttons should be functional when clicked", () => {
        expect(true)
    })

    test("all operation buttons should be functional when clicked", () => {
        expect(true)
    })

    test("when Erase(C) is clicked, the last character should be removed from the formula", () => {
        expect(true)
    })

    test("when Clear(AC) is clicked, all the formula elements should be removed", () => {
        expect(true)
    })

})